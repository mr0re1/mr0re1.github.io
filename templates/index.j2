<html>
<head>
    <title>{{ index.title }}</title>
</head>
<body>
    <h1>{{ index.title }}</h1>
    <ul>
    {% for post in posts %}
        <li><a href="{{ post.url }}">{{ post.title }}</a></li>
    {% endfor %}
    </ul>
</body>
</html>